# 贪吃蛇

一个用 C 语言编写的 Windows 控制台贪吃蛇小游戏。

## 目录结构

```
贪吃蛇/
├── Makefile          # 构建脚本(编译 / 运行 / 清理)
├── README.md         # 项目说明
├── src/              # 源代码
│   └── snake.c       # 贪吃蛇主程序(单文件实现)
├── include/          # 头文件目录(本项目为单文件,暂未使用;拆分为多模块时 .h 放这里)
├── build/            # 构建中间产物目录(如 .o 文件)
├── bin/              # 编译产物目录(可执行文件)
└── docs/             # 文档目录(设计说明、使用说明等)
```

## 编译与运行

```bash
make          # 编译,生成 bin/snake.exe
make run      # 编译并运行
make clean    # 清理产物
```

也可以手动编译:

```bash
gcc -Wall -Wextra -O2 -o bin/snake.exe src/snake.c
```

## 操作方式

| 按键 | 功能 |
|---|---|
| WASD / 方向键 | 控制蛇头移动 |
| 空格 Space | 暂停 / 继续 |
| R | 游戏结束后重新开始新一局 |
| Q 或 Esc | 退出游戏(游戏中直接退出;结算画面按 Q 退出) |

## 游戏规则

- 蛇吃到食物 `*` 增长一节,得分 +10;
- 撞到墙壁(`#`)或撞到自己即游戏结束;
- 蛇越长移动速度越快(帧间隔从 130ms 递减至 50ms);
- 蛇身填满整个场地即获胜。

## 版本历史

| 版本 | 日期 | 说明 |
|---|---|---|
| **v0.4.3** | 2026-08-12 | 游戏中 Q/Esc 改为暂停(仅结算画面按 Q 退出);地图调整为 30×20 视觉更协调;窗口设置尽力而为,适配 CMD/PowerShell/Git Bash |
| **v0.4.2** | 2026-08-12 | 修复吃食物后左上角墙壁消失、地图残留大量 `o` 的 bug(蛇身数组垃圾坐标) |
| **v0.4.1** | 2026-08-12 | 修复控制台窗口未缩小、游戏画面只占窗口一角的问题 |
| **v0.4.0** | 2026-08-12 | 游戏结束后按 R 开始新一局,按 Q 退出;修复重开时蛇长沿用上一局 |
| **v0.3.0** | 2026-08-12 | 游戏结束后必须按 Q 才退出,避免误触跳过结算画面 |
| **v0.2.0** | 2026-08-12 | 修复中文乱码、窗口尺寸贴合地图、贴墙吃食物墙面缺一块 |
| **v0.1.0** | 2026-08-12 | 初始版本:贪吃蛇核心玩法 + 标准 C 项目结构 |

详细的逐项变更(含修复根因)见 [CHANGELOG.md](CHANGELOG.md)。

每次代码修改后均需同步更新 CHANGELOG.md 与本表,
记录新增功能与修复的 bug。

