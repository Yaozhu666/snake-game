# 更新日志 (Changelog)

本文件记录项目的所有重要变更,遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)
格式,版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v0.4.4] - 2026-08-12

### 文档
- README 新增「版本历史」表,汇总 v0.1.0 ~ v0.4.3 各版本变更摘要。
- README 标题与目录结构去掉 "project 001" 字样,只保留「贪吃蛇」。
- GitHub 仓库描述改为中文(由用户在网页端手动修改)。

## [v0.4.3] - 2026-08-12

### 变更
- 游戏中按 **Q / Esc** 不再退出,改为与空格一样**切换暂停**;仅游戏结束后的
  结算画面按 Q 退出,方便随时暂停游戏。
- 地图尺寸由 40×20 调整为 **30×20**:控制台字符格本身竖长(高≈2 倍宽),
  原比例在部分终端视觉上横向偏密,30×20 更协调。
- 窗口设置改为"尽力而为":在 CMD / PowerShell 中自动贴合地图尺寸,
  在 Git Bash 等无法调整窗口的终端中不报错、游戏照常可玩;
  仅当窗口小到地图显示不全时才提示,窗口过大(留白)不再打扰。

## [v0.4.2] - 2026-08-12

### 修复
- 修复吃掉第一个食物后左上角墙壁 `#` 消失的问题:蛇身前移循环只复制
  `snakeLen-1` 个元素,吃食物长度+1 后数组尾部残留未初始化坐标(默认为
  (0,0)),下一帧"擦蛇尾"把左上角擦成空格。
- 修复地图上残留大量形似蛇身的 `o` 的问题:同一根因——真正的蛇尾未被
  正确擦除,每吃一个食物残留一节;现改为从 `snakeLen` 开始整体移位,
  吃食物时旧尾自动保留为新尾,数组尾部不再有垃圾坐标。

### 变更
- 蛇身坐标数组增加 1 个冗余槽(`MAX_LEN + 1`),防止整体移位时越界。

## [v0.4.1] - 2026-08-12

### 修复
- 修复控制台窗口未缩小、游戏画面只占窗口一角的问题:`setupConsole()` 调整
  设置顺序(先扩大缓冲区 → 设置窗口 → 再收缩缓冲区),并先还原最大化窗口,
  使窗口尺寸真正贴合地图(44×26)。
- 新增 `checkWindow()` 校验:若窗口仍未能缩到目标尺寸,在画面底部提示
  调整控制台字体大小。

## [v0.4.0] - 2026-08-12

### 新增
- 游戏结束后可立即开始新一局:结算画面按 **R** 键重新开局,按 **Q** 键退出,
  不再直接退出程序,方便连续游玩。

### 修复
- 修复重开一局时蛇身长度沿用上一局的问题:`initGame()` 新增 `snakeLen = 3`
  重置,每局都从 3 节开始。

### 变更
- `main()` 改为外层 `while (again)` 循环:每局结束后停留在结算画面,
  按 R 重开、按 Q 退出。

## [v0.3.0] - 2026-08-12

### 修复
- 修复游戏结束后误触任意键直接退出的问题:结算画面必须按 Q 键才会退出,
  避免误触导致来不及看到分数就结束。
- 优化结算画面:居中显示 Game Over、最终得分与"按 Q 键退出游戏..."提示。

### 变更
- `main()` 中游戏结束等待循环由 `while (!kbhit())` 改为仅响应 Q 键退出的循环。

## [v0.2.0] - 2026-08-12

### 修复
- 修复控制台中文乱码:源码为 UTF-8,而 Windows 控制台默认 GBK(代码页 936),
  新增 `setupConsole()` 调用 `SetConsoleOutputCP(CP_UTF8)` 切换输出代码页。
- 修复游戏画面只占窗口一角、四周大片空白:在 `setupConsole()` 中用
  `SetConsoleScreenBufferSize` + `SetConsoleWindowInfo` 将窗口/缓冲区尺寸
  调整为与地图匹配(44×26),并关闭滚动条。
- 修复贴墙吃食物后墙面缺一块:`step()` 原先无条件擦除蛇尾,吃到食物时
  蛇身变长、旧尾巴本应保留,却被误打为空格;现改为仅在未吃到食物时擦尾。

### 变更
- `main()` 中新增 `setupConsole()` 调用。

## [v0.1.0] - 2026-08-12

### 新增
- Windows 控制台版贪吃蛇小游戏(单文件 C 实现,依赖 `windows.h` / `conio.h`)。
- 核心玩法:WASD 或方向键移动、空格暂停、Q/Esc 退出、吃食物增长并加分、
  撞墙或撞到自己游戏结束,蛇越长速度越快。
- 标准 C 项目目录结构:src / include / build / bin / docs,附 Makefile。
- 文档:README.md、docs/设计说明.md。
